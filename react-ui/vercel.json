{
  "version": 2,
  "builds": [
    {
      "src": "./index.html",
      "use": "@vercel/static-build"
    }
  ],
  "routes": [{ "src": "/[^.]+", "dest": "/", "status": 200 }],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
